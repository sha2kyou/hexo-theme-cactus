<% if (theme.cc && theme.cc.enabled && theme.cc.post.enabled) { %>
    <hr/>
    本文是<%= config.author %>的原创文章，转载请提前告知作者并且标明出处。<br/>
    内容遵循 <%= theme.cc.description %> (<%= theme.cc.name %>) 协议。
<% } %>